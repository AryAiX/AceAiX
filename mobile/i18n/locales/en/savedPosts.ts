/**
 * Saved posts (app/saved.tsx).
 *
 * A short, familiar screen: title, and an empty state for before anything's
 * been saved yet.
 */
export const savedPosts = {
  // ── Saved (app/saved.tsx) ────────────────────────────────────────────────
  title: 'Saved',
  emptyTitle: 'Nothing saved yet',
  emptyBody: 'Posts you save will show up here so you can find them again later.',
};
